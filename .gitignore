node_modules/
dist/
.astro/
.env
.env.local
.DS_Store
.vscode/
*.log

# Local notes (not for public repo)
details.md
chat-with-gemini.md

# Original large image files kept locally as backup; the optimized JPGs in public/process/ are what we ship
_raw-images/

# Local Worker secrets — never commit
.dev.vars
scripts/*.local.sql

# Agent worktrees and scratch notes — local only
.claude/worktrees/
notes.txt
notes.txt.save
chat-with-gemini
